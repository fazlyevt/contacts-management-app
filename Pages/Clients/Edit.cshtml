@page
@model ContactsManagementApp.Pages.Clients.EditModel

@{
    ViewData["Title"] = "Изменить организацию";
}

<h1>Изменить организацию</h1>

<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Client.ClientId" />
            <div class="form-floating mb-3">
                <input asp-for="Client.Name" class="form-control" placeholder=@Html.DisplayNameFor(o => o.Client.Name) />
                <label asp-for="Client.Name" class="control-label"></label>
                <span asp-validation-for="Client.Name" class="text-danger small"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Сохранить" class="btn btn-primary" />
                <a class="btn btn-primary" asp-page="Index">Назад</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
